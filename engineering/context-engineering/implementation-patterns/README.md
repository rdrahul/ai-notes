# Implementation Patterns

Reusable patterns for context engineering — sliding window memory, hierarchical summarization, context prioritization, structured context schemas, and multi-source merging.
